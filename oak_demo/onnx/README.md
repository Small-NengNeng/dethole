# ONNX 模型目录

将 MMYOLO 训练后导出的坑洞检测 ONNX 放在此目录，例如：

- `epoch_230.onnx`

导出方式见 [oak_demo/README.md](../README.md) 中「从 MMYOLO 导出 ONNX」一节。

也可从网盘 `mmyolo_work_dirs.tar.gz` 解压后，在 `work_dirs/` 中查找已导出的 `.onnx` 并复制到本目录。
